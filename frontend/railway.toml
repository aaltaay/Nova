# DEPRECATED (2026-08-05): Nova no longer deploys the frontend via Railway.
# Optional static hosting is Vercel; API is local / Desktop sidecar only.
# See AGENTS.md §8 and README.md Deploy.

[build]
builder = "RAILPACK"

[deploy]
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
