---
description: Enforce truthful GitHub issue metadata, PR-first delivery, delete merged PR branches, strict gates, and complete-only closure
alwaysApply: true
---

# GitHub delivery lifecycle

For issue, code, config, CI, security, or rule work, load `.cursor/skills/github-delivery/SKILL.md`.

## Required

1. Inspect the issue body, labels, owner, project, milestone, relationships, and Development links before work.
2. Code/config/CI/rule changes use a feature branch and PR. Direct `master` push is only for status-only work or explicit user instruction.
3. Every actionable issue has kind, priority, domain, concrete acceptance/Next, human owner while active, and a Nova Delivery project item when access exists.
4. Milestones and relationships are conditional facts, never filler.
5. Use `Closes #NNN` only when the whole issue is complete. Partial work uses `Refs #NNN`, gets an evidence comment, and leaves the issue open.
6. Before merge, run relevant pytest, Ruff, ESLint, Vitest, build, Playwright/browser, doc, agent-contract, shared-neighbor gates, and wait for Desktop pack (Windows EXE artifact `Nova-Setup-vNNN.exe`) when that job applies.
7. After closing a deferred issue, run `py -3 tools/deferred_log.py refresh-index` and ship the refreshed index.
8. After a PR is merged or closed, **delete the head branch in the same session** if it is still on origin. GitHub `delete_branch_on_merge` is on (backup sweep). Do not skip the check. Run `git fetch origin --prune`, then `py -3 tools/stale_pr_branches.py`. If the head is still listed, run `git push origin --delete <branch>` (or the GitHub "Delete branch" control) and prune again. Never delete `master` / `main`, and never delete a branch that still has an **open** PR.
9. **Master branch protection** is required: no force-push, no deletion, admins included, and PRs into `master` must pass `Backend tests`, `Frontend build`, `Frontend E2E`, and `Agent contract`. Do not require reviews (solo deadlock). Do not require a PR to push (status-only + AI news digest). Check with `py -3 tools/master_branch_protection.py check`. Apply with `apply` only when the token has Administration and the account has GitHub Pro on this private repo.

If GitHub plan or token permissions prevent Projects or branch protection, report the exact limitation. Never claim the metadata or protection exists. Private `aaltaay/Nova` needs GitHub Pro for this feature -- do not make the repo public to unlock it.
