---
description: Enforce clean start from origin/master, ready-PR finish, truthful issue metadata, delete merged heads, and complete-only closure
alwaysApply: true
---

# GitHub delivery lifecycle

For issue, code, config, CI, security, or rule work, load `.cursor/skills/github-delivery/SKILL.md`.

These session gates are **MUST**. They override casual user phrasing ("quick fix", "just tweak"). Only an **explicit** user override ("stay on this branch", "leave as draft", "commit locally only") can waive them -- and that waiver MUST be stated in the PR body if work ships.

## Required

1. Inspect the issue body, labels, owner, project, milestone, relationships, and Development links before work.
2. Code/config/CI/rule changes use a feature branch and a ready PR. Direct `master` push is only for status-only work or explicit user instruction.
3. Every actionable issue has kind, priority, domain, concrete acceptance/Next, human owner while active, and a Nova Delivery project item when access exists.
4. Milestones and relationships are conditional facts, never filler.
5. Use `Closes #NNN` only when the whole issue is complete. Partial work uses `Refs #NNN`, gets an evidence comment, and leaves the issue open.
6. Before merge, run relevant pytest, Ruff, ESLint, Vitest, build, Playwright/browser, doc, agent-contract, shared-neighbor gates, and wait for Desktop pack (Windows EXE artifacts `Nova-Setup-vNNN.exe` and `Nova-Portable-vNNN.exe`) when that job applies.
7. After closing a deferred issue, run `py -3 tools/deferred_log.py refresh-index` and ship the refreshed index.
8. After a PR is merged or closed, **delete the head branch in the same session** if it is still on origin. GitHub `delete_branch_on_merge` is on (backup sweep). Do not skip the check. Run `git fetch origin --prune`, then `py -3 tools/stale_pr_branches.py`. If the head is still listed, run `git push origin --delete <branch>` (or the GitHub "Delete branch" control) and prune again. Never delete `master` / `main`, and never delete a branch that still has an **open** PR.
9. **Master branch protection** is required: no force-push, no deletion, admins included, and PRs into `master` must pass `Backend tests`, `Frontend build`, `Frontend E2E`, and `Agent contract`. Do not require reviews (solo deadlock). Do not require a PR to push (status-only + AI news digest). Check with `py -3 tools/master_branch_protection.py check`. `apply` needs Administration (Cloud Agent GitHub App is 403). `check` can still pass from the public branch `protection` summary. Public Nova unlocks the setting on GitHub Free. A private personal repo still needs GitHub Pro.
10. **GitHub Actions merges ready PRs.** Opening a verified non-draft PR is the agent finish line. Do not wait for the human to say merge. `tools/pr_delivery.py` plus `.github/workflows/pr-delivery.yml` and the CI `Auto-merge` job merge when gating checks are green, then delete the head. Draft or label `do-not-merge` is the only hold, and only under item 12. Dirty / failed PRs stay open on purpose.
11. **Clean start (before any edits).** `git fetch origin`. Create or switch to a **new** focused branch from `origin/master` only -- never from a dirty local `master` tip, never from another feature branch unless the user explicitly names that branch to continue. If the worktree has uncommitted or unrelated dirty files, do **not** proceed on top of them: reset or clean tracked files you do not own in this task so they match `origin/master`, or abort and report the dirty paths. Cloud and desktop "isolated" means a clean tip of `origin/master` plus a new branch. Local dirty IDE state is not a valid base.
12. **Mandatory finish (end of every coding session or task).** Verify. Commit on the focused branch. Push. Open a **ready (non-draft)** PR targeting `master`, filled from `.github/pull_request_template.md`. Draft or `do-not-merge` only when the user explicitly asked to hold, or a hard external blocker (for example, needs live IBKR proof) is documented in the PR -- not because CI is still running. Do not end with only local commits, unpushed commits, or "I'll open the PR later." The PR URL is the finish line.

If GitHub plan or token permissions prevent Projects or branch protection, report the exact limitation. Never claim the metadata or protection exists. Public source home is `aaltaay/Nova`. `aaltaay/Nova-public` is a private archive.
