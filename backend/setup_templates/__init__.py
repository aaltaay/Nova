"""Setup templates (ADR 029): every number each setup runs on, and the operator's
named variations of them -- the parameter catalogue, the template store and its
routes. The scanner reads the templates; nothing here places an order."""
