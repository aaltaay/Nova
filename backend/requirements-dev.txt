-r requirements.txt
pytest
pytest-asyncio>=0.24,<2
ruff==0.15.21
pip-audit
# CVSS v4 scoring library — used by tools/security_audit.py to classify pip-audit findings
cvss>=2.6,<3
# Optional: schemathesis for OpenAPI fuzz testing (many transitive deps — install separately if needed)
# schemathesis>=4.30,<5
# Stall diagnosis: when /api/health times out with 0 CPU, dump the wedged process:
#   py -3 -m pip install py-spy
#   py -3 -m py_spy dump --pid <nova-api-pid>
# (admin shell may be required on Windows)
py-spy>=0.4.0,<1
# Opt-in replay benchmark RSS measurements.
psutil==7.2.2
