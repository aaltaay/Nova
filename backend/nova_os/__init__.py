"""
Nova OS -- what remains after ADR 025 retired the verdict and the mode ladder.

  codes / events_db / events -- the stable vocabulary and the append-only
  receipts log. Kill switch trips, risk halts and archive-health failures still
  write here; the attention strip and the alert channels read it.

The decide() verdict, the signal / confirm / auto_paper ladder, the staged
approval queue and the executor's restart recovery were removed (ADR 025).
"""
